name: CI

on:
  pull_request:
  push:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # The repo commits node_modules, so there is no install step.
      - name: Build the static site
        run: npm run build
      - name: Run the browser tests
        run: npm test
        env:
          CHROME_PATH: /usr/bin/google-chrome
